module.exports = {
    userMiddleware: require('./user.middleware'),
    userAuthMiddleware: require('./user-auth.middleware'),
    doctorMiddleware: require('./doctor.middleware'),
    mainMiddleware: require('./main.middleware')
};
